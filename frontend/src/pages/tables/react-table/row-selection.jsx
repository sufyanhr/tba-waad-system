import { Typography, Box } from '@mui/material';
const ReactTableRowSelection = () => (
  <Box><Typography variant="h3">React Table RowSelection</Typography></Box>
);
export default ReactTableRowSelection;

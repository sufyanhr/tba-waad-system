import { Typography, Box } from '@mui/material';
const ReactTableColumnVisibility = () => (
  <Box><Typography variant="h3">React Table ColumnVisibility</Typography></Box>
);
export default ReactTableColumnVisibility;

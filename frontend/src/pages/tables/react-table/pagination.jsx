import { Typography, Box } from '@mui/material';
const ReactTablePagination = () => (
  <Box><Typography variant="h3">React Table Pagination</Typography></Box>
);
export default ReactTablePagination;

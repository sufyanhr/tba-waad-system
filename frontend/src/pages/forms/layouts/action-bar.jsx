import { Typography, Box } from '@mui/material';
const FormsLayoutActionBar = () => (
  <Box><Typography variant="h3">Forms Layout ActionBar</Typography></Box>
);
export default FormsLayoutActionBar;

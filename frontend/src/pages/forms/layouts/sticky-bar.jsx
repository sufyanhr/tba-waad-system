import { Typography, Box } from '@mui/material';
const FormsLayoutStickyBar = () => (
  <Box><Typography variant="h3">Forms Layout StickyBar</Typography></Box>
);
export default FormsLayoutStickyBar;

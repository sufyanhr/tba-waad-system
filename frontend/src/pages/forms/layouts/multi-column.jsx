import { Typography, Box } from '@mui/material';
const FormsLayoutMultiColumn = () => (
  <Box><Typography variant="h3">Forms Layout MultiColumn</Typography></Box>
);
export default FormsLayoutMultiColumn;

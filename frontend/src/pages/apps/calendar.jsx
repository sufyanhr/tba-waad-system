import { Typography, Box } from '@mui/material';

const AppCalendar = () => {
  return (
    <Box>
      <Typography variant="h3">Calendar</Typography>
      <Typography variant="body1" sx={{ mt: 2 }}>Calendar application coming soon...</Typography>
    </Box>
  );
};

export default AppCalendar;

import { Typography, Box } from '@mui/material';

const AppChat = () => {
  return (
    <Box>
      <Typography variant="h3">Chat</Typography>
      <Typography variant="body1" sx={{ mt: 2 }}>Chat application coming soon...</Typography>
    </Box>
  );
};

export default AppChat;

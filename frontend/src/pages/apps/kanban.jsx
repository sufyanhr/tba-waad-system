import { Typography, Box } from '@mui/material';

const AppKanban = () => {
  return (
    <Box>
      <Typography variant="h3">Kanban</Typography>
      <Typography variant="body1" sx={{ mt: 2 }}>Kanban application coming soon...</Typography>
    </Box>
  );
};

export default AppKanban;

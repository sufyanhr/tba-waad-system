import { Typography, Box } from '@mui/material';

const WidgetData = () => {
  return (
    <Box>
      <Typography variant="h3">Widget Data</Typography>
      <Typography variant="body1" sx={{ mt: 2 }}>Widget data coming soon...</Typography>
    </Box>
  );
};

export default WidgetData;

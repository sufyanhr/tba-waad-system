package com.waad.tba.model;

public enum Role {
    ADMIN,          // مدير النظام الكامل
    REVIEW,         // شركة المراجعة (وعد)
    INSURANCE,      // شركة التأمين (الواحة)
    ORG,            // شركة تابعة (مصنع الأسمنت، المصرف...)
    PROVIDER        // المستشفيات والعيادات
}
